%p
  = t("mail.hello", :name => @resource.email)
%p= t("mail.lock.warning")
%p= t("mail.lock.instruction")
%p= link_to t("devise.unlock_action"), unlock_url(@resource, :unlock_token => @resource.unlock_token)

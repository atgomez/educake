- form_id = "modal-goal-#{@goal.id}"
$("#new-goal-container").html("#{escape_javascript(render(:partial => "form", :locals => {:goal => @goal, :student => @student, :id => form_id}))}");
$("##{form_id}").modal('show');

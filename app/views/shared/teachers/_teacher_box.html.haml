.student-container.border-teacher.align-border-single.clearfix.toolbar
  .avatar-container.well
    = image_tag(@teacher.photo_url, :class => "default-avatar-teacher")
    
  .info-teacher.well.pull-left
    .pull-left
      %strong.name
        = @teacher.full_name
      - if !@teacher.classroom.blank?
        .classroom
          %span= "Classroom:"
          %span= @teacher.classroom
      - if !@teacher.email.blank?
        .email
          %span= "Email:"
          %span= @teacher.email
      - if !@teacher.classroom.blank?
        .phone
          %span= "Phone:"
          %span= @teacher.phone
  
  .pull-right
    = link_to("", edit_admin_teacher_path(:id => @teacher, :user_id => @admin), 
              :class => "icon-pencil color-pen", :title => "Edit", 
              :style => "display:inline-block", :remote => true)
    = link_to("", { :action => 'destroy', :controller => 'admin/teachers', 
                    :id => @teacher, :user_id => @admin, :admin_id => @admin }, 
                  :data => { :confirm => t('admin.teacher.confirm_delete') }, 
                  :class => "icon-trash color-pen", :title => "Delete", 
                  :style => "display:inline-block", :method => :delete )

-# Place holder for teacher form dialog
#new-form-teacher
